{
  "name": "mgsio3-lattice-dynamics",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "description": "MgSiO3 lattice-dynamics engine, after Jackson (2009) — v2",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/three": "0.170.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.5"
  },
  "dependencies": {
    "three": "^0.170.0"
  }
}
